{
    "name": "core",
    "version": "1.0.0",
    "scripts": {
        "db": "docker run --name cloak-core -e POSTGRES_PASSWORD=pwd -e POSTGRES_DB=database -d -p 5432:5432 postgres",
        "deldb": "docker rm -f cloak-core",
        "seed": "pnpm run db && go run cmd/seed/main.go",
        "start": "docker container start cloak-core && go run cmd/server/main.go"
    },
    "license": "ISC"
}
