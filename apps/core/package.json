{
    "name": "core",
    "version": "1.0.0",
    "scripts": {
        "devDb": "docker run --name cloak-core -e POSTGRES_PASSWORD=pwd -e POSTGRES_DB=database -d -p 5432:5432 postgres",
        "devDelDb": "docker rm -f cloak-core",
        "devDbSchema": "go run cmd/dev/db-schema/main.go",
        "devSeedDb": "pnpm run db && go run cmd/seed/main.go",
        "start": "docker container start cloak-core && go run cmd/server/main.go"
    },
    "license": "ISC"
}
